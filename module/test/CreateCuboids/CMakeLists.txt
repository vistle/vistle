add_module(
    CreateCuboids
    "creates cuboids out of an unstructured grid with (3D vector) mapped data where the grid coordinates are the cuboid centers and the mapped data the edge lengths in x, y, z"
    CreateCuboids.h
    CreateCuboids.cpp)
