add_module(
    CreateCuboids
    "creates cuboids out of a grid with (3D vector) mapped data where the grid's coordinates are the cuboid centers and its mapped data the edge lengths in x, y, z"
    CreateCuboids.h
    CreateCuboids.cpp)
