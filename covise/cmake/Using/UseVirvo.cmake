MACRO(USE_VIRVO)
  IF(NOT VIRVO_USED)
    SET(VIRVO_USED TRUE)
    ADD_DEFINITIONS(-DVV_APPLICATION_BUILD)
    INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/3rdparty/deskvox/virvo)
    SET(EXTRA_LIBS ${EXTRA_LIBS} virvo)
  ENDIF(NOT VIRVO_USED)
ENDMACRO(USE_VIRVO)
